import "package:flutter/material.dart";
import "../../../../core/widgets/app_state_views.dart";

/// Placeholder untuk screen "Search Global".
///
/// Struktur folder (data/domain/presentation) sudah siap; implementasi
/// UI & state management (Riverpod provider, usecase, repository) akan
/// dikerjakan pada fase pengembangan modul ini.
class SearchScreen extends StatelessWidget {
  const SearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderScreen(title: "Search Global");
  }
}
